# Deliberately descoped

- **LLM extraction path — cut entirely (partner, Window 1).** Compliance review
  is required before building against any third-party model and will take days;
  the partner explicitly refused to set a provisional default. Prototype is
  deterministic-only. The integration point stays specified in the technical
  doc so it can be switched on after compliance rules, but nothing is built
  against it and no key is required to run.
